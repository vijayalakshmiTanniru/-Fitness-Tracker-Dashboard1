import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer = () => (
  <Box textAlign="center" mt={5} mb={3}>
    <Typography variant="body2" color="textSecondary">
      © 2025 Fitness Tracker Dashboard
    </Typography>
  </Box>
);

export default Footer;
